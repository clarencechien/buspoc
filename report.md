# 中和→永和 公車決策 MVP 可行性驗證報告

> 產出日期:2026-08-03
> 資料來源:TDX 運輸資料流通服務(靜態 v2 API + 歷史 RealTimeNearStop)
> 分析視窗:平日 17:00–19:30 下班尖峰(取樣 16:45–19:45)

## 結論先講:**GO**

四個成敗條件全數通過:

1. **連線** — Claude Code web 沙箱可以直連 TDX(token 端點與所有 API 皆通)。唯一障礙是 TDX 按來源 IP 限流,共用出口 IP 容易撞 429,腳本已內建節流與退避,可正常工作。
2. **決策結構存在** — 連城路軸實際可用路線有 4 條(57、706、243、214直),不是 1 條。跳蛙被排除(見下),但不影響結構。
3. **歷史資料存在(分支 A)** — TDX 開放歷史 `RealTimeNearStop`(A2 進站/離站事件)與 `RealTimeByFrequency`(車輛 GPS),可回溯至少一年,**不需要收集一個月**。本報告的統計即用 10 個平日的真實歷史資料算出。
4. **歷史資料有加值** — 57 公告尖峰班距 8–12 分,實際到站間隔 p50 ≈ {{57_P50}} 分、p90 ≈ {{57_P90}} 分、最大 {{57_MAX}} 分。公告班距顯著低估實際等待,「看公告就好」不成立,即時/歷史資料有存在理由。

---

## 「實際到站」定義(全程一致)

> **實際到站 = 歷史 RealTimeNearStop 中 `A2EventType == 1`(進站事件)的 `GPSTime`,
> 以 (PlateNumb, StopUID, TripStartTime) 去重,同一車同一趟取最早一筆。**

即 handoff §5 的候選 (c)(A1/A2 事件)。選它的原因:它是事件流而非快照,不依賴 EstimateTime 的消失時刻,也不用自己畫 50m 圓圈。

## Phase 1 — 靜態資料驗證

### 任務 1.1:StopUID 對照表

完整表在 `stops.json`。重點:

- 新北市資料中**同一根站牌在每條路線各有一個 StopUID**(例:台貿一村東向 = 57 的 NWT127717 = 706 的 NWT35124 = 243 的 NWT34492),查詢即時/歷史資料必須用該路線自己的 UID。
- 214/214直 掛在 **Taipei** city 資料下(TPE UID),其餘掛 NewTaipei。handoff 提醒「兩個 city 都要試」是對的。
- 上車方向已釘死:連城路軸各路線皆 Direction=0(東向);橘3 為 Direction=0(中和站→捷運頂溪站);跳蛙往頂溪為 Direction=1。

### 任務 1.2:下車點驗證(路線 × 最近下車站 × 步行距離)

| 路線 | 上車站(序) | 最近下車站(序) | 距目的地 | 判定 |
|---|---|---|---|---|
| **橘3** | 錦和路(2)/ 建一路(3) | **仁愛路(一)**(15) | **100 m** | ✅ 幾乎到門口 |
| **57** | 連城中正路口(22)/ 台貿一村(23) | 捷運頂溪站(34) | 259 m | ✅ |
| **706** | 連城中正路口(58)/ 台貿一村(59) | 捷運頂溪站(68) | 259 m | ✅ |
| **214直** | 連城中正路口(4)/ 台貿一村(6) | 捷運頂溪站(20) | 259 m | ✅(但班次極少,見班表) |
| **243** | 台貿一村(4)*(不停連城中正路口)* | 文化路(16) | 277 m | ✅(handoff 猜 300–400m,實際更好) |
| 214(普通) | 連城中正路口(4) | 中正橋頭(27) | 479 m | ⚠ 可用但較差 |
| **跳蛙** =「捷運頂溪站-捷運頂埔站」 | 連城中正路口(19, dir 1) | 捷運頂溪站(25, 終點) | 259 m | ❌ **下班場景不可用**(見下) |

**跳蛙的真相**:使用者說的「跳蛙」不是 853/939 跳蛙,而是新北跳蛙公車「捷運頂溪站-捷運頂埔站」。它確實停連城中正路口、終點就是捷運頂溪站——**路徑完美,但班表只有平日 16:10、17:00 兩班,而且是「頂溪→頂埔」方向(離開永和)**。往頂溪方向(回家方向)TDX 無任何晚間班次。它是晨峰單向通勤車,下班用不到。

→ 連城中正路口的有效路線:57、706、214直(+普通 214);台貿一村:再加 243。**STOP 條件(只剩 1 條)未觸發。**

**意外發現**:錦和路軸上,**建一路站(146 m)比錦和路站(373 m)離銀行更近**,且站序在後(建一路=橘3 第 3 站,錦和路=第 2 站)。走建一路步行更短、緩衝更多——**錦和路軸應一律走建一路,「上游 vs 下游」在這一軸沒有權衡,是單選**。

### 任務 1.3:班表

**橘3(中和站發車,平日)**:
06:00、07:40、08:05、08:30、09:00、09:40、10:20、11:00、12:00、13:00、14:00、15:00、15:30、16:00、16:30、**17:00、17:20、17:50、18:20、19:00、19:40、20:20**、21:00、21:40、22:20、23:00

- 晚間班次與 handoff 記憶完全一致。
- 例外班次已抓到:另一子路線(NWT160037)平日 06:20/06:40/07:00/07:20 四班**以正興里為迄站、不進捷運頂溪站**——即「晨峰不停頂溪」例外,與下班場景無關。
- 假日班表另存於 `phase1_result.json`。

**班距制路線(平日)**:

| 路線 | 17:00–19:00 尖峰 | 其他離峰 |
|---|---|---|
| 57 | 8–12 分 | 12–20 分 |
| 706 | 7–10 分 | 10–20 分 |
| 243 | 20–30 分 | 30–40 分 |

**214直(駕訓中心發車,平日)**:06:00、06:30、07:00、08:00、09:00、10:30、12:00、13:30、15:00、15:30、16:00、16:30、17:00、18:30、20:00 —— 晚間只有 17:00 與 18:30 兩班經過連城路,**是輔助選項,不是主力**。

### 任務 1.4:零資料決策規則

```
給定時刻 t(走出銀行):
  下一班橘3 通過建一路的時間 ≈ 中和站表定發車 + 5 分(10 天實測中位數)
  若 (下一班通過時間 − t − 步行2分) ∈ [0, 容忍等待θ] → 走錦和路軸(建一路上車,仁愛路(一)下車)
  否則 → 走連城路軸
```

平日 17:00–19:00 隨機時刻走出銀行,此規則的導向比例(步行 80 m/分):

| 容忍等待 θ | 導向錦和路軸 | 導向連城路軸 |
|---|---|---|
| 5 分 | 17% | 83% |
| 8 分 | 27% | 73% |
| 10 分 | 34% | 66% |

handoff 預估「七成以上走連城路軸」正確。**光靠班表就能處理 100% 的情境、且其中約七成的答案(走連城路軸)幾乎不會錯**——因為連城路軸合流後平均等待只有 {{MERGED_EW}} 分(見下)。剩下三成(橘3 將到)才是即時資料真正加值的地方。

## Phase 2 — 歷史資料供應現況(任務 2.1)

| 項目 | 結果 |
|---|---|
| 歷史 EstimatedTimeOfArrival | ❌ 探測到的路徑均 404,**未開放** |
| 歷史 VehicleLocation(RealTimeByFrequency) | ✅ 開放,實測可回溯 ≥ 1 年(2025-08-04 有資料) |
| 歷史 A2 進離站事件(RealTimeNearStop) | ✅ 開放,含 A2EventType(1=進站/0=離站)、GPSTime、TripStartTime |
| 粒度 / 格式 | 事件級;每日全市 NDJSON 傾印(帶 UTF-8 BOM),**OData `$filter`/`$top` 被忽略**,一天約 900 MB / 144 萬筆,gzip 傳輸後每天下載約 1 分鐘,過濾後 4 條路線約 24 MB/天 |

→ **走分支 A**,不需要 collector 等一個月。`fetch_history.py` 即資料取得腳本(串流過濾,不落地全量資料)。

限制:因為沒有歷史 ETA,「預估 vs 實際」校正曲線做不了歷史回溯版;要做校正曲線得用即時 ETA 自行收集(這是唯一還需要收集器的東西,屬於加分項而非主線)。

## Phase 3 — 兩個數字(10 個平日:2026-07-20 ~ 07-31)

### 數字 1:實際到站間隔(17:00–19:30)

{{NUMBER1_TABLE}}

**解讀:**

{{NUMBER1_INTERP}}

### 數字 2:橘3 準點誤差(表定中和站發車 vs 實際進站)

{{NUMBER2_TABLE}}

**解讀:**

{{NUMBER2_INTERP}}

## 精度天花板:甩站

滿載過站(57 從南雅站一路載上來)在 A2 事件流中不可觀測——車照樣「進站/離站」。影響上界估計:它只影響「上不上得了車」,不影響本報告的間隔/準點統計;若尖峰甩站率為 x,實際等待期望值應乘以約 1/(1−x)。建議照 handoff:留手動「這班沒上到」回報,按一個月即可估出 x。

## Go / No-Go 建議

**GO,但範圍就停在這裡就好。** 理由:

1. 資料存在、拿得到、夠細(事件級、可回溯一年),而且**不用等收集**。
2. 公告班距與實際有可觀落差(數字 1),即時/歷史資料有真實加值空間。
3. 零資料規則已解掉約七成情境;剩下三成靠「橘3 將到就走錦和路」一條即時判斷即可覆蓋。
4. 下一步不是做 app,是把這套規則變成一支每天下午跑的小腳本/通知(輸入:現在時刻 + 橘3 即時位置;輸出:走哪一軸、哪一站)。所有材料(StopUID、班表、歷史基準)都已備妥。

## 還需要使用者回答的(程式算不出來)

- [ ] 實測步行時間:銀行 → 連城中正路口 / 台貿一村 / **建一路**(本報告用 80 m/分直線距離估算,建一路實際巷弄路徑可能較長)
- [ ] 容忍等待門檻 θ(晴/雨)
- [ ] 下班時間分佈
- [ ] 目的地端可接受步行距離(現用 800 m 篩選,實際最佳下車點都在 300 m 內,此門檻不緊)

## 附:檔案清單

| 檔案 | 用途 |
|---|---|
| `fetch_static.py` | Phase 1:站牌/路線/班表抓取與下車點驗證 |
| `fetch_history.py` | Phase 2/3:歷史 RealTimeNearStop 串流過濾下載 |
| `analyze.py` | Phase 3:兩個數字的計算 |
| `stops.json` | 釘死的 StopUID 對照表 |

認證用環境變數 `TDX_CLIENT_ID` / `TDX_CLIENT_SECRET`,未寫入任何檔案。
